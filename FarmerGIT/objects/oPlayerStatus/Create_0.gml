head = instance_create_depth(-100, -100, 10, oEquipSlot);
head.equipSlot = HEAD;
head.handler = id;
offHand = instance_create_depth(-100, -100, 10, oEquipSlot);
offHand.equipSlot = OFFHAND;
offHand.handler = id;
chest = instance_create_depth(-100, -100, 10, oEquipSlot);
chest.equipSlot = CHEST;
chest.handler = id;
mainHand = instance_create_depth(-100, -100, 10, oEquipSlot);
mainHand.equipSlot = MAINHAND;
mainHand.handler = id;
legs = instance_create_depth(-100, -100, 10, oEquipSlot);
legs.equipSlot = LEGS;
legs.handler = id;
consumable = instance_create_depth(-100, -100, 10, oEquipSlot);
consumable.equipSlot = CONSUMABLE;
consumable.handler = id;

consumeButton = ObjectiveButton(OP_ConsumeItem, "E", "Consume Item");